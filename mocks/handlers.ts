export const handlers = [
  // rest.get('/api/bodyweight-history', (_, res, ctx) => (
  //   res(ctx.status(200), ctx.json<Bodyweight[]>([{ value: 50, date: '1970-01-01', type: 'official', _id: generateId() }]))
  // ))
]
